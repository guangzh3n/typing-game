# 🎮 儿童打字游戏

一个适合儿童使用的打字练习游戏，使用 Node.js 后端和 React 前端构建。

## 功能特点

- 🎯 三个难度级别（简单、中等、困难）
- ⏱️ 实时计时和统计
- 📊 显示打字速度（WPM）和准确率
- 🎨 适合儿童的友好界面
- 📱 响应式设计，支持移动设备
- 🔊 字母和单词语音发音
- 🎵 答对/答错音效
- 🔇 声音开关控制

## 技术栈

- **后端**: Node.js + Express
- **前端**: React.js
- **样式**: CSS3 (渐变、动画)
- **部署**: Docker + Docker Compose

## 快速开始

### 使用 Docker（推荐）

```bash
# 使用 Docker Compose 启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

访问 http://localhost:3001

### 本地开发

#### 1. 安装依赖

```bash
# 安装后端和前端依赖
npm run install-all
```

或者分别安装：

```bash
# 安装后端依赖
npm install

# 安装前端依赖
cd client
npm install
cd ..
```

#### 2. 开发模式运行

```bash
# 同时启动后端和前端（推荐）
npm run dev

# 或者分别启动
# 终端 1: 启动后端
npm run server

# 终端 2: 启动前端
npm run client
```

- 后端服务器: http://localhost:3001
- 前端应用: http://localhost:3000

#### 3. 构建生产版本

```bash
# 构建前端
npm run build
```

构建完成后，前端文件会在 `client/build` 目录中。

## 部署

### Docker 部署（推荐）

详细说明请查看 [DEPLOY.md](./DEPLOY.md) 中的 Docker 部署部分。

### cPanel 部署

详细说明请查看 [DEPLOY.md](./DEPLOY.md) 中的 cPanel 部署部分。

## 项目结构

```
piano-game/
├── server/
│   └── index.js          # Express 后端服务器
├── client/
│   ├── src/
│   │   ├── components/
│   │   │   ├── TypingGame.js      # 主游戏组件
│   │   │   ├── LevelSelector.js   # 难度选择组件
│   │   │   └── GameStats.js       # 统计显示组件
│   │   ├── utils/
│   │   │   ├── soundEffects.js    # 音效工具
│   │   │   └── speechUtils.js     # 语音工具
│   │   ├── App.js                 # 主应用组件
│   │   └── App.css                # 主样式
│   └── build/                     # 构建输出目录
├── Dockerfile                      # Docker 镜像构建文件
├── docker-compose.yml              # Docker Compose 配置
├── package.json                    # 后端依赖
└── README.md                       # 说明文档
```

## API 端点

- `GET /api/words?level=easy&count=10` - 获取单词列表
- `GET /api/levels` - 获取难度级别信息
- `GET /api/health` - 健康检查

## 环境变量

- `PORT` - 服务器端口（默认: 3001）
- `NODE_ENV` - 环境模式（development/production）

## 许可证

MIT
